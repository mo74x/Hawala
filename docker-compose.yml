version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    container_name: hawala-postgres
    environment:
      POSTGRES_USER: hawala_user
      POSTGRES_PASSWORD: hawala_password
      POSTGRES_DB: hawala_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: hawala-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata: